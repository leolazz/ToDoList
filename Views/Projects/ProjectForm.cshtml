@model ToDoList.DTOs.ProjectDto
@{
    ViewData["Title"] = "New Project";
}

@using (Html.BeginForm("Save", "Project"))
{
    <div class="form-group">
        @Html.LabelFor(t => t.Title, new { @class = "text-white-50" })
        @Html.TextBoxFor(t => t.Title, new { @class = "form-control", placeholder = "Enter the name of your Project" })
    </div>
    <div class="form-group">
        @Html.LabelFor(t => t.Description, new { @class = "text-white-50" })
        @Html.TextAreaFor(t => t.Description, new { @class = "form-control", rows = "5", cols = "50", placeholder = "Describe your Project" })
    </div>

    
    @*<div class="form-group">
        @Html.
        @Html.LabelFor(t => t.Tasks, new { @class = "text-white-50" })
        @Html.ListBoxFor(t => t.Tasks, new SelectList(), new { @class = "form-control", rows = "1", cols = "20", placeholder = "Define the Qualifers for this Task" })
    </div>*@
    @*<div class="form-group">
        @Html.LabelFor(t => t.Outcomes.Outcomes, new { @class = "text-white-50" })
        @Html.TextAreaFor(t => t.Outcomes.Outcomes, new { @class = "form-control", rows = "1", cols = "20", placeholder = "Define an Outcome of this Task" })
    </div>
    <div class="form-group">
        @Html.LabelFor(t => t.EndDate, new { @class = "text-white-50" })
        @Html.TextBoxFor(t => t.EndDate, new { @type = "date", @class = "form-control datepicker", @Value = Model.EndDate.ToString("yyyy-MM-dd") })
    </div>*@
    <button type="submit" class="btn btn-primary btn-lg btn-block">Save</button>
}