@model ToDoList.Models.Task
@{
    ViewData["Title"] = "New Task";
}

@using (Html.BeginForm("New", "Tasks"))
{
    <div class="form-group">
        @Html.LabelFor(t => t.Title)
        @Html.TextBoxFor(t => t.Title, new { @class = "form-control", placeholder="Enter the name of your task" })
    </div>
    <div class="form-group">
        @Html.LabelFor(t => t.Details.Details)
        @Html.TextAreaFor(t => t.Details.Details, new { @class = "form-control", rows="5", cols="50", placeholder="Describe your task" }) 
    </div>
    <div class="form-group">
        @Html.LabelFor(t => t.EndDate)
        @Html.TextBoxFor(t => t.EndDate, new { @type = "date", @class = "form-control datepicker", @Value = Model.EndDate.ToString("yyyy-MM-dd") })
    </div>
    <button type="button" class="btn btn-primary btn-lg btn-block">Create Task</button>
}